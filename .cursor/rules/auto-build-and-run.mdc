---
description: 修改完成后自动执行 build + test:gate + 重启服务，全部通过后用户刷新即可测试
alwaysApply: true
---

# 修改后自动构建、测试、重启服务流程

## 核心要求

每次代码修改完成后，**必须**自动执行以下完整流程，用户刷新浏览器即可直接测试：

1. **自动构建（`npm run build`）** — 改了 `components/`、`src/`、Vite 配置、`utils/`、`services/` 等任何影响生产包的内容时**必须执行**。
2. **回归门禁测试（`npm run test:gate`）** — 改了四大验收域（面板/生成结果/拖拽/Node Details）相关文件时**必须执行**且全部通过。
3. **自动重启服务** — build 和测试全部通过后，自动终止旧服务进程，重启新服务：
   - **终止旧进程**：`taskkill /PID <PID> /F`（Windows）或 `kill <PID>`（Linux），释放端口 3001
   - **启动新服务**：`npm start` — 运行 `dist/` + `server.js`，端口 3001
4. **交付确认** — 服务成功启动后，向用户汇报测试结果，用户刷新浏览器即可测试。

## 执行顺序

```
修改代码完成
  ↓
npm run build（必须执行）
  ↓
构建成功 → npm run test:gate（四大域相关改动必须执行）
  ↓
测试全部通过 → 终止旧服务进程（释放端口 3001）
  ↓
启动新服务（npm start）
  ↓
服务启动成功 → 用户刷新浏览器即可测试
```

## 失败处理

- **构建失败**：修复代码后重新 build，直至成功。
- **测试失败**：分析错误原因，修复代码后重新执行测试，直至全部通过。
- **端口占用**：自动检测并终止占用端口 3001 的进程，然后重新启动服务。
- **服务启动失败**：排查错误原因，修复后重新启动，直至服务正常运行。

## 禁止事项

- **禁止**在未 build 前声称任务完成。
- **禁止**在测试未通过时交付给用户。
- **禁止**询问用户「是否要 build / 启动 / 重启服务」，应自动执行。
- **禁止**在服务未成功启动时交付给用户。

仅在构建失败、测试失败、或服务启动失败时说明阻塞原因；成功时直接告知用户服务已重启，刷新即可测试。
